RSpec.describe Ajax::Datatables::Rails::Alt::Api do
  it "has a version number" do
    expect(Ajax::Datatables::Rails::Alt::Api::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
